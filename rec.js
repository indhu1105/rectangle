let length = +prompt("enter the length");
        let width = +prompt("enter the width");
        let result;

        function area() {
          result = length * width;
          console.log(result);
          return result;
        }